graph_html_path: data/08_reporting/knowledge_graph.html

# Number of most-recent records (by admission date) treated as the "new batch"
# when running the graph_update pipeline to demonstrate ontology updates.
update_batch_size: 5000

node_colors:
  condition: "#D9506A"
  medication: "#FFC900"
  insurer: "#00FFBC"
  admission_type: "#4A9EF5"
  test_result: "#F07828"
  blood_type: "#A870DC"
