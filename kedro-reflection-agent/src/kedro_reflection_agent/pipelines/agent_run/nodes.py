"""Nodes for the ``agent_run`` pipeline.

Generates personalised B2B outreach emails for each (customer, product) eval
case and emits a Langfuse trace per call. Nodes will be added in a later slice.
"""
